{{ form_start(mediaform, {'action': path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')), 'method': 'POST'}) }}
<h2 style="font-size:20px;color:#575757;">Choisir une image sur votre ordinateur</h2>
{{ form_row(mediaform.Link) }}
<h2 class="mt-3" style="font-size:20px;color:#575757;">Ou entrer le code de partage d'une video</h2>
{{ form_row(mediaform.videoEmbed, {'attr' : {'style' : 'min-height:150px;'}}) }}
<button type="submit" class="btn btn-primary mt-3">Ajouter un media</button>
{{ form_end(mediaform) }}